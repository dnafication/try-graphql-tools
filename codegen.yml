overwrite: true
schema: "src/index.ts"
documents: null
require:
  - ts-node/register
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  # ./graphql.schema.json:
  #   plugins:
  #     - "introspection"
